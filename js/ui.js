// UI Components - Stats cards, word list panel, modals, shared utilities - placeholder
// To be implemented in Phase 3

function renderStatsCard(label, value) {
  // Placeholder
  return `<div class="stats-card"><span>${label}</span><span>${value}</span></div>`;
}
